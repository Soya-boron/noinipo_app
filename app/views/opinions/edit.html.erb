<h1>意見編集</h1>

<%= form_tag("/opinions/#{@opinion.id}/update") do %>
    <p>意見</p>
    <textarea name="content"> <%="#{@opinion.content}"%> </textarea>
    <input type="submit" value="変更">
<%end%>

<%= link_to("削除","/opinions/#{@opinion.id}/destroy",{method:"post"})%>