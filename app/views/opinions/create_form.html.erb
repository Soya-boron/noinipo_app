<h1>意見投稿</h1>

<%= form_tag("/opinions/#{@theme.id}/create") do %>
    <p>テーマ:<%= @theme.name %></p>
    <p>意見</p>
    <textarea name="content"></textarea>

    <input type="submit" value="投稿">
<%end%>